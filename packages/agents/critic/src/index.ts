export const FORGE_AGENT_CRITIC_VERSION = "0.0.0";

// Semantic half (call site 3) deferred this sitting — structural half lives in @forge/core/critic.
export { assessCoverage, classGaps, structuralScore, verdict, COVERAGE_FLOOR } from "@forge/core";
